specVersion: "v1beta"
serviceId: "deliveryapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "deliveryapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
